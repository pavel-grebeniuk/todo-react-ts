import * as React from "react";
import TextField from "@material-ui/core/TextField";

export const CreateTodo: React.FC = () => {
  return (
    <TextField id="standard-basic" label="Add new todo"/>
  );
};
